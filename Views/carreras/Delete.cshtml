@model CRUDescuelaMVC.Models.carrera

@{
    ViewBag.Title = "Eliminar Carrera";
}

<h2>Eliminar Carrera</h2>

<h3>¿Estás seguro que deseas eliminar esta carrera?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.nom_c)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nom_c)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.durac_c)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.durac_c)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Eliminar" class="btn btn-default" /> |
            @Html.ActionLink("Regresar al listado", "Index")
        </div>
    }
</div>
