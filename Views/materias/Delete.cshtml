@model CRUDescuelaMVC.Models.materia

@{
    ViewBag.Title = "Eliminar Materia";
}

<h2>Eliminar materia</h2>

<h3>¿Estás seguro que deseas eliminar esta materia?</h3>
<div>
    <h4>materia</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.nom_m)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nom_m)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.cred_m)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.cred_m)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Eliminar" class="btn btn-default" /> |
            @Html.ActionLink("Regresar al listado", "Index")
        </div>
    }
</div>
